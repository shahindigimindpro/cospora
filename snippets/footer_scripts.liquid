<!-- Footer scripts -->
<script src="{{ 'vendor.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'api.jquery.js' | shopify_asset_url }}" async="async"></script>
<script src="{{ 'option_selection.js' | shopify_asset_url }}" async="async"></script>
{%  render 'new-currency' %}
<script src="{{ 'jquery.sticky.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'cospora.min.js' | asset_url }}" defer="defer"></script>
{% if template contains 'collection' %}
<script>
  $(document).ready(function(){
    $('.dropdown-cat a').each(function(){
      if($(this).hasClass('active')) {
      	$(this).parents('li').addClass('open');
      	$('.dropdown-cat').show();
      }
      if($(this).parents('li').hasClass('open')) {
      	$('.dropdown-cat').show();
      }
    });
  })
</script>
{% endif %}